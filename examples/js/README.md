# Node.js Example

Prerequisites
- Node.js 16+

Setup
1. Install dependencies:
   npm install

2. Set API key:
   export API_KEY="your_api_key_here"  # macOS / Linux
   setx API_KEY "your_api_key_here"   # Windows (then reopen terminal)

Run
   node demo.js
